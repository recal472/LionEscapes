// Can use the below to apply device screen (not viewport) ratio to an element
// const screenAR = (el) => el.style.aspectRatio = screen.width / screen.height

// screenAR(document.querySelector("#element"));


//idk

// let LionTitleDiv = document.getElementById("front-lion-headline");
// let LionTitleDivHeight = parseInt(window.getComputedStyle(LionTitleDiv).offsetHeight);
// LionTitleDiv.style.fontSize =  100 + "px";

